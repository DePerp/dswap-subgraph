{
  "name": "dswap-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dswap-graph",
    "create-local": "graph create --node http://localhost:8020/ dswap-graph",
    "remove-local": "graph remove --node http://localhost:8020/ dswap-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dswap-graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.85.0",
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
